const PrototypeExtension = require("prototype-extension")

class TestModule
{
	static testMethod(self)
	{
		return TestModule.name + "." + testMethod.name;
	}
}

Object._.extendWith(TestModule)

module.exports.isImported = true