import test from 'ava'
import PrototypeExtension from "../prototype-extension"

test.todo("Initialize the accessor with another identifier than \"_\"")
test.todo("Reset all the extensions on a prototype and an accessor")
test.todo("Reset all the extensions for everything")
